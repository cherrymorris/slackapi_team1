Hi <%= @i_user.email %>,
Welcome to the Slack App! Click on the link below to confirm your account:
<% if @ruby_ui.present? %>
  <%= "#{CONSTANTS::SLACK_APP_RUBY_LINK}?email=#{@i_user.email}&workspaceid=#{@i_workspace.id}&channelid=#{@i_channel.id}&workspace_name=#{@i_workspace.workspace_name}&channel_name=#{@i_channel.channel_name}" %>
<% else %>
  <%= "#{CONSTANTS::SLACK_APP_FLUTTER_LINK}?email=#{@i_user.email}&workspaceid=#{@i_workspace.id}&channelid=#{@i_channel.id}&workspace_name=#{@i_workspace.workspace_name}&channel_name=#{@i_channel.channel_name}" %>
<% end %>